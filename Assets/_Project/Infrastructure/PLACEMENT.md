# Placement

This module is located under `Infrastructure` according to `Docs/Ru/02_ProjectStructure.md:172-188`:

> Infrastructure является адаптером между проектом и Unity.
>
> Любой код, работающий непосредственно с API Unity, должен находиться здесь.

The same section explicitly lists `SceneManagement`, `Input`, `Reflection`, and `Rendering`. `Docs/Ru/01_Architecture.md:71-83` also lists:

> - Save System

This note covers non-commentable or generated assets such as assembly definitions, render pipeline assets, Input Actions, and `PlayerInputActions.cs`.
