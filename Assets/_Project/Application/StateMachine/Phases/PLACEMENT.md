# Placement

These phases are located under `Application/StateMachine` according to `Docs/Ru/02_ProjectStructure.md:98-116`:

> StateMachine знает только о жизненном цикле фаз.

The same section explicitly lists `GamePhase`, `SceneGamePhase`, and `OverlayPhase` as StateMachine components. This note also covers the folder and assembly metadata that cannot contain source comments.
